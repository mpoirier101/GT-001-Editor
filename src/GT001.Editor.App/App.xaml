<Application
    x:Class="GT001.Editor.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#142236" />
        <SolidColorBrush x:Key="PanelBackground" Color="#1D3350" />
        <SolidColorBrush x:Key="PanelAltBackground" Color="#263B58" />
        <SolidColorBrush x:Key="RailBackground" Color="#2D3F58" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2F86FF" />
        <SolidColorBrush x:Key="WarmBrush" Color="#F7B64D" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#AEB9C8" />

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource PanelAltBackground}" />
            <Setter Property="BorderBrush" Value="#45607F" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="4" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Margin" Value="4" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Application.Resources>
</Application>
